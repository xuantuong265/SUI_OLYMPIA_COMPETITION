export enum STEP_GAME_QUIZ {
  LOGIN = "login",
  LOBBY = "lobby",
  ROOM = "room",
  READY_GAME = "readyGame",
  END_GAME = "endGame",
}

export enum TYPE_MESSAGE {
  LOGIN = 0,
  LOBBY = 1,
  JOIN_ROOM = 3,
  CREATE_ROOM = 4,
  READY_GAME = 5,
  PLAY_GAME = 7,
}
